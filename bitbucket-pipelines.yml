image: cvkurt/gcxp:latest
pipelines:
  default:
    - step:
        name: build and test
        script:
          - mkdir build
          - cd build
          - cmake -G Ninja ..
          - ninja
          - ninja junit
